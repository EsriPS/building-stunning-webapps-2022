// Framework and third-party non-ui

// App components

// JSON & Styles
import { StyledUser } from './User-styled';

// Third-party components (buttons, icons, etc.)

const User = () => {
  return <StyledUser>Jack</StyledUser>;
};

export default User;
